---
hide_table_of_contents: true
---

# `MarkdownTextSplitter`

만약 콘텐츠가 Markdown 형식이라면 `MarkdownTextSplitter` 클래스를 사용하여 콘텐츠를 Markdown 헤더를 기준으로 문서로 분할할 수 있습니다. 예를 들어, 다음과 같은 Markdown 콘텐츠가 있다고 가정해봅시다.:

```markdown
# Header 1

This is some content.

## Header 2

This is some more content.

# Header 3

This is even more content.
```

그럼 `MarkdownTextSplitter`는 콘텐츠를 세 개의 문서로 분할할 것입니다:

```typescript
import { MarkdownTextSplitter } from "langchain/text_splitter";

const text = `# Header 1

This is some content.

## Header 2

This is some more content.

# Header 3

This is even more content.`;

const splitter = new MarkdownTextSplitter();

const output = await splitter.createDocuments([text], {
  metadata: "something",
});
/*
[
  {
    "pageContent": "# Header 1\n\nThis is some content.",
    "metadata": "something"
  },
  {
    "pageContent": "## Header 2\n\nThis is some more content.",
    "metadata": "something"
  },
  {
    "pageContent": "# Header 3\n\nThis is even more content.",
    "metadata": "something"
  }
]
*/
```
